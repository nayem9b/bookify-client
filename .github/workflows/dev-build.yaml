name: Dev Build & Artifact

on:
  push:
    branches: ["main"]
    paths:
      - "src/**"
      - "package.json"
      - "Dockerfile"
      - ".github/workflows/dev-build.yaml"
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

permissions:
  contents: read
  packages: write
  actions: write

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.new_version }}
      build-timestamp: ${{ steps.timestamp.outputs.timestamp }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name github-actions[bot]
          git config user.email github-actions[bot]@users.noreply.github.com

      # Generate version based on commit count
      - name: Generate version
        id: version
        run: |
          LATEST_TAG=$(git tag -l --sort=-v:refname | head -n 1)

          if [ -z "$LATEST_TAG" ] || ! [[ "$LATEST_TAG" =~ ^v?[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            LATEST_TAG="0.0.0"
          fi

          CLEAN_TAG=${LATEST_TAG#v}
          MAJOR=$(echo $CLEAN_TAG | cut -d. -f1)
          MINOR=$(echo $CLEAN_TAG | cut -d. -f2)
          PATCH=$(echo $CLEAN_TAG | cut -d. -f3)

          NEW_PATCH=$((PATCH + 1))
          NEW_VERSION="${MAJOR}.${MINOR}.${NEW_PATCH}"

          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "Version: $NEW_VERSION"

      - name: Generate timestamp
        id: timestamp
        run: echo "timestamp=$(date -u +'%Y%m%d-%H%M%S')" >> $GITHUB_OUTPUT

      # Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"
          cache: "npm"

      # Install dependencies
      - name: Install dependencies
        run: npm install

      # # Run tests
      # - name: Run tests
      #   continue-on-error: true
      #   run: npm test -- --watchAll=false --passWithNoTests

      # Build the application
      - name: Build application
        run: npm run build
        env:
          CI: false

      # Create artifact from build output
      - name: Create build artifact
        run: |
          cd build
          tar -czf ../bookify-client-${{ steps.version.outputs.new_version }}.tar.gz .
          cd ..

      # Upload artifact to GitHub
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: bookify-client-${{ steps.version.outputs.new_version }}
          path: bookify-client-${{ steps.version.outputs.new_version }}.tar.gz
          retention-days: 30

      # Create GitHub Release with artifact
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.new_version }}
          name: Release v${{ steps.version.outputs.new_version }}
          body: |
            ## Build Artifact
            - **Version**: ${{ steps.version.outputs.new_version }}
            - **Build Timestamp**: ${{ steps.timestamp.outputs.timestamp }}
            - **Artifact**: bookify-client-${{ steps.version.outputs.new_version }}.tar.gz
            - **Commit**: ${{ github.sha }}
          files: bookify-client-${{ steps.version.outputs.new_version }}.tar.gz
          draft: false
          prerelease: true

      # Trigger staging deployment
      - name: Trigger staging deployment
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'staging-qa-release.yaml',
              ref: 'main',
              inputs: {
                version: '${{ steps.version.outputs.new_version }}'
              }
            })

      # Summary
      - name: Print build summary
        run: |
          echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Version**: ${{ steps.version.outputs.new_version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Build Timestamp**: ${{ steps.timestamp.outputs.timestamp }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Artifact**: bookify-client-${{ steps.version.outputs.new_version }}.tar.gz" >> $GITHUB_STEP_SUMMARY
          echo "- **Release**: [v${{ steps.version.outputs.new_version }}](https://github.com/${{ github.repository }}/releases/tag/v${{ steps.version.outputs.new_version }})" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit SHA**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
